@model IEnumerable<IS_11_Coursova.Models.TeacherRank>

@{
    ViewBag.Title = "Index";
}

<h2>Teachers Rank</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TeacherRankId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TeacherRankId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TeacherRankId })
        </td>
    </tr>
}

</table>
